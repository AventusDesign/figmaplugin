{
  "name": "figma-inspect-plugin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.ui.json --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.25.12",
    "typescript": "^5.0.0"
  }
}
